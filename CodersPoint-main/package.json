{
  "name": "coderspoint",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix frontend && npm run build --prefix frontend && npm install --prefix backend && npx --prefix backend prisma generate",
    "start": "npx --prefix backend prisma migrate deploy && npm start --prefix backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
